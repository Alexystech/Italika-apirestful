spring:
  application:
    name: italika-apirest
  datasource:
    username: 
    password:
    driver-class-name: org.postgresql.Driver
    url:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: postgresql
server:
  port: 8080
